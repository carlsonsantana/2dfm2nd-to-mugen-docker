"""Convert 2D Fighter Maker 2nd characters into MUGEN 1.0 characters.

This package holds the Python half of the pipeline (JSON/text/image handling);
binary MUGEN artifacts (SFF, SND) are built with MUGEN's own tools under Wine.
"""
