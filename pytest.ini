[pytest]
# `src` is on the path for local runs; in the container the package is installed
# at /opt/converter via PYTHONPATH, so this extra entry is simply ignored there.
pythonpath = src
testpaths = tests
